import styled from "styled-components";
import {fontConfigs} from "../../global";

export {}

export const ChoosePlayerText = styled.div`
  font-family: ${fontConfigs.fontFamily};
  font-style: normal;
  font-size: ${fontConfigs.sizes.normal};
  color: ${fontConfigs.fontColor};
  margin-top: 150px;
  margin-bottom: 30px;
`;

export const ModeratorButtonContainer = styled.div`
  margin-top: 40px;
`;